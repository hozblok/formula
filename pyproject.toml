[build-system]
requires = ["setuptools>=61", "wheel", "pybind11>=2.10,<3"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = "cp39-* cp310-* cp311-* cp312-* pp39-* pp310-*"
skip = "*-musllinux_i686 *-win32 pp*-win_*"
build-verbosity = 1
test-requires = ["pytest"]
# --import-mode=importlib avoids pytest prepending {project} to sys.path,
# which would otherwise let the source-tree formula/ package shadow the
# installed wheel (the source tree has no compiled _formula.so).
test-command = "python -m pytest --verbose --import-mode=importlib {project}/tests"
test-skip = "*universal2:arm64 pp*"

[tool.cibuildwheel.macos]
archs = ["x86_64", "arm64", "universal2"]
environment = { MACOSX_DEPLOYMENT_TARGET = "10.15" }

[tool.cibuildwheel.linux]
archs = ["x86_64"]

[tool.cibuildwheel.windows]
archs = ["AMD64", "x86"]
